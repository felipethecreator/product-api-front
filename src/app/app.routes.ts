import { Routes } from '@angular/router';
import { HelloWorld } from './components/pages/hello-world/hello-world';

export const routes: Routes = [
    {
        path: "helloworld",
        component: HelloWorld
    }
];
